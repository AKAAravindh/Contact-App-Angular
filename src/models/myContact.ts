// model for holding data from add contact page
// here '?' means - if it has value...then

export class MyContact {
    id?: string;
    name?: string;
    email?: string;
    photo?: string;
    mobile?: string;
    company?: string;
    title?: string;
    groupId?: string;
}